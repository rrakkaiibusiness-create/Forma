import { Info } from 'lucide-react'
import type { Profile } from '../lib/types'
import { generateTraining } from '../lib/training'
import { TrainingPlan } from '../components/training/TrainingPlan'
import { Badge } from '../components/ui/Badge'
import { experienceLabels, goalLabels } from '../lib/constants'
export function TrainingPage({ profile }: { profile: Profile }) { const workouts = generateTraining(profile); return <div className="grid gap-5 xl:grid-cols-[minmax(0,1fr)_300px]"><div><div className="mb-4 flex flex-wrap items-center gap-2"><Badge tone="good">{profile.trainingDays} дні / тиждень</Badge><Badge>{experienceLabels[profile.experience]}</Badge><Badge>{goalLabels[profile.goal]}</Badge></div><TrainingPlan workouts={workouts}/></div><aside className="space-y-4"><div className="rounded-2xl border border-white/[.08] bg-[#141715] p-5"><h3 className="text-sm font-semibold text-white">Правило прогресії</h3><p className="mt-2 text-xs leading-relaxed text-zinc-400">Залишай 1–2 повтори в запасі. Коли виконуєш верхню межу повторів у всіх робочих підходах — додай мінімальний крок ваги.</p><div className="mt-4 h-1.5 overflow-hidden rounded-full bg-white/[.06]"><div className="h-full w-2/3 rounded-full bg-lime"/></div><div className="mt-2 flex justify-between text-[10px] text-zinc-600"><span>Техніка</span><span>Повтори</span><span>Вага</span></div></div><div className="flex gap-3 rounded-2xl border border-sky-300/10 bg-sky-300/[.04] p-4"><Info size={16} className="shrink-0 text-sky-300"/><p className="text-xs leading-relaxed text-zinc-400">Розподіляй важкі дні через 48–72 години. Більше — не завжди краще; стабільність виграє.</p></div></aside></div> }
