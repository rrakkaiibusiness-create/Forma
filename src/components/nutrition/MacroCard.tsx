import type { LucideIcon } from 'lucide-react'
export function MacroCard({ label, value, unit, target, icon: Icon, accent = 'text-lime' }: { label: string; value: number; unit: string; target?: string; icon: LucideIcon; accent?: string }) { return <div className="rounded-2xl border border-white/[.07] bg-white/[.025] p-4"><div className="flex items-center justify-between"><span className="text-xs font-medium text-zinc-500">{label}</span><Icon size={16} className={accent}/></div><div className="mt-3"><span className="text-2xl font-bold tracking-tight text-white">{value}</span><span className="ml-1 text-xs text-zinc-500">{unit}</span></div>{target && <p className="mt-1 text-[11px] text-zinc-600">{target}</p>}</div> }
