import type { InputHTMLAttributes } from 'react'
export function Input({ label, hint, className = '', ...props }: InputHTMLAttributes<HTMLInputElement> & { label?: string; hint?: string }) { return <label className="block"><span className="mb-2 block text-sm font-medium text-zinc-300">{label}</span><input className={`h-11 w-full rounded-xl border border-white/10 bg-white/[.04] px-3.5 text-sm text-white outline-none transition placeholder:text-zinc-600 focus:border-lime/60 focus:ring-2 focus:ring-lime/10 ${className}`} {...props}/>{hint && <span className="mt-1.5 block text-xs text-zinc-500">{hint}</span>}</label> }
