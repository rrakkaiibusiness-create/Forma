import { Check, Moon, Scale, X, Zap } from 'lucide-react'
import type { ProgressLog as Log } from '../../lib/types'
export function ProgressLog({ log }: { log: Log }) { return <article className="rounded-xl border border-white/[.07] bg-white/[.025] p-4"><div className="flex items-start justify-between"><div><p className="text-xs text-zinc-500">{new Date(log.date + 'T12:00:00').toLocaleDateString('uk-UA', { day: 'numeric', month: 'long' })}</p><b className="mt-1 block text-lg text-white">{log.weight} кг</b></div><div className="flex gap-1.5"><span className={`grid size-7 place-items-center rounded-lg ${log.dietDone ? 'bg-lime/10 text-lime' : 'bg-white/5 text-zinc-600'}`} title="Раціон">{log.dietDone ? <Check size={14}/> : <X size={14}/>}</span><span className={`grid size-7 place-items-center rounded-lg ${log.workoutDone ? 'bg-lime/10 text-lime' : 'bg-white/5 text-zinc-600'}`} title="Тренування">{log.workoutDone ? <Check size={14}/> : <X size={14}/>}</span></div></div><div className="mt-3 flex gap-4 border-t border-white/[.06] pt-3 text-xs text-zinc-400"><span className="flex items-center gap-1.5"><Moon size={13}/> {log.sleep} год</span><span className="flex items-center gap-1.5"><Zap size={13}/> {log.energy}/5</span><span className="flex items-center gap-1.5"><Scale size={13}/> голод {log.hunger}/5</span></div>{log.note && <p className="mt-3 text-xs text-zinc-500">{log.note}</p>}</article> }
