import { useState } from 'react'
import { CalendarDays, ChevronDown, ChevronUp } from 'lucide-react'
import type { Workout } from '../../lib/types'
import { Card } from '../ui/Card'
import { ExerciseCard } from './ExerciseCard'
export function TrainingPlan({ workouts }: { workouts: Workout[] }) { const [open, setOpen] = useState(0); return <div className="space-y-3">{workouts.map((workout, index) => <Card key={workout.title} className={open === index ? 'border-lime/20' : ''}><button className="flex w-full items-center justify-between gap-4 p-4 text-left sm:p-5" onClick={() => setOpen(open === index ? -1 : index)}><div className="flex items-center gap-3"><span className={`grid size-10 place-items-center rounded-xl ${open === index ? 'bg-lime text-ink' : 'bg-white/[.06] text-zinc-400'}`}><CalendarDays size={18}/></span><div><p className="text-[10px] font-bold uppercase tracking-[.14em] text-zinc-500">{workout.day} · {workout.exercises.length} вправ</p><h3 className="mt-0.5 font-semibold text-white">{workout.title} <span className="font-normal text-zinc-600">— {workout.focus}</span></h3></div></div>{open === index ? <ChevronUp size={18} className="text-lime"/> : <ChevronDown size={18} className="text-zinc-600"/>}</button>{open === index && <div className="space-y-2 border-t border-white/[.07] p-3 sm:p-4">{workout.exercises.map((exercise, i) => <ExerciseCard key={`${exercise.name}-${i}`} exercise={exercise} index={i}/>)}</div>}</Card>)}</div> }
